{
  "$schema": "../gen/schemas/capability.json",
  "identifier": "widget",
  "description": "Capabilities for the Aether desktop widget",
  "windows": ["widget"],
  "permissions": [
    "core:default",
    "core:window:allow-set-always-on-top",
    "core:window:allow-start-dragging"
  ]
}
